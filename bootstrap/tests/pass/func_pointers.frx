fn func() -> i32
{
    return 1;
}

fn foo() -> i32
{
    return -1;
}

struct FuncWrapper
{
    pub func: fn() -> i32;
}

fn main() -> i32
{
    let wrapper: FuncWrapper;
    wrapper.func = func;

    let f = foo;

    return wrapper.func() + f();
}
