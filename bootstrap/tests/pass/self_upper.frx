struct Foo
{
    pub x: i32;
}

impl Foo
{
    fn new(x: i32) -> Self
    {
        let foo = Self
        {
            x: x;
        };

        return foo;
    }
}

fn main() -> i32
{
    let foo = Foo::new(0);

    return foo.x;
}
