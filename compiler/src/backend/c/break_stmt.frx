export void transpile_break_stmt(Transpiler* transpiler,
    ASTBreakStmt* break_stmt)
{
    assert(transpiler != nullptr);

    assert(break_stmt != nullptr);

    fprintf(transpiler->file, "break;\n");
}
