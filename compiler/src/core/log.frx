void log_error(char* filepath, SourceLocation location, char* format, ...)
{
    printf("[ERROR]:%s:%zu:%zu: ", filepath, location.line, location.column);

    va_list args;
    va_start(args, format);
    vprintf(format, args);
    va_end(args);
}
