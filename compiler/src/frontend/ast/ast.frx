extern struct FILE;

extern
{
    i32 fprintf(mut FILE* stream, char* format, ...);
}

export void print_depth(mut FILE* file, usize depth)
{
    frx::assert(file != nullptr);

    mut usize i;
    for(i = 0; i < depth; i = i + 1)
    {
        fprintf(file, "    ");
    }
}
