import "../../core/arena.frx";
export import "../../core/list.frx";

namespace AST
{
    export struct Compound
    {
        List nodes;
    }

    namespace Compound
    {
        export Compound* new(Arena* arena)
        {
            mut Compound* compound = Arena::alloc(arena, frx::sizeof(Compound));

            List::init(&compound->nodes);

            return compound;
        }

        export void add_node(mut Compound* compound, void* node)
        {
            frx::assert(compound != nullptr);

            List::push(&compound->nodes, node);
        }
    }
}
