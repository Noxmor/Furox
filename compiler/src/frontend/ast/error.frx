import "../../core/arena.frx";
import "../core/source_range.frx";

export struct Error
{
    SourceRange range;
}

export Error* error_new(Arena* arena)
{
    Error* error = arena_alloc(arena, frx::sizeof(Error));

    return error;
}
