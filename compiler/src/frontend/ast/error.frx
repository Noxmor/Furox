import "../../core/arena.frx";
import "../core/source_range.frx";

namespace AST
{
    export struct Error
    {
        SourceRange range;
    }

    namespace Error
    {
        export Error* new(Arena* arena)
        {
            Error* error = Arena::alloc(arena, frx::sizeof(Error));

            return error;
        }
    }
}
