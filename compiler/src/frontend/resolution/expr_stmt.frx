import "../ast/expr_stmt.frx";
import "../core/parser.frx";
import "expr.frx";

export void expr_stmt_resolve_symbols(Parser* parser, ExprStmt* expr_stmt)
{
    frx::assert(parser != nullptr);

    frx::assert(expr_stmt != nullptr);

    expr_resolve_symbols(parser, expr_stmt->expr);
}
