import "../ast/unary_expr.frx";
import "../core/parser.frx";
import "expr.frx";

export void unary_expr_resolve_symbols(Parser* parser, UnaryExpr* unary_expr)
{
    frx::assert(parser != nullptr);

    frx::assert(unary_expr != nullptr);

    expr_resolve_symbols(parser, unary_expr->operand);
}
