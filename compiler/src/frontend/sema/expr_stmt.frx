import "expr.frx";
import "../core/parser.frx";
import "../ast/expr_stmt.frx";

export void expr_stmt_sema(Parser* parser, ExprStmt* expr_stmt)
{
    frx::assert(parser != nullptr);

    frx::assert(expr_stmt != nullptr);

    expr_sema(parser, expr_stmt->expr);
}
