import "expr.frx";
import "../core/parser.frx";
import "../ast/unary_expr.frx";

export void unary_expr_sema(Parser* parser, UnaryExpr* expr)
{
    frx::assert(parser != nullptr);

    frx::assert(expr != nullptr);

    expr_sema(parser, expr->operand);
}
