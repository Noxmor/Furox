import "expr.frx";
import "../core/parser.frx";
import "../ast/expr_stmt.frx";

export void analyze_expr_stmt(Parser* parser, AST::ExprStmt* expr_stmt)
{
    frx::assert(parser != nullptr);

    frx::assert(expr_stmt != nullptr);

    analyze_expr(parser, expr_stmt->expr);
}
