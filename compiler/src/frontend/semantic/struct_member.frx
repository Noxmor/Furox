import "../core/parser.frx";
import "../syntax/struct_member.frx";

export void analyze_struct_member(Parser* parser, ASTStructMember* member)
{
    assert(parser != nullptr);

    assert(member != nullptr);
}
