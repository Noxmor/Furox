import "expr.frx";
import "../core/parser.frx";
import "../ast/unary_expr.frx";

export void analyze_unary_expr(Parser* parser, AST::UnaryExpr* expr)
{
    frx::assert(parser != nullptr);

    frx::assert(expr != nullptr);

    analyze_expr(parser, expr->operand);
}
