export import "../ast/break_stmt.frx";
import "../core/parser.frx";

export BreakStmt* break_stmt_parse(Parser* parser)
{
    parser_eat(parser, TokenType::KW_BREAK);

    parser_eat(parser, TokenType::SEMICOLON);

    return break_stmt_new(&parser->arena);
}
