export import "../ast/char_literal.frx";
import "../core/parser.frx";

namespace AST::CharLiteral
{
    export CharLiteral* parse(Parser* parser)
    {
        frx::assert(parser != nullptr);

        char* literal = parser->token->identifier;

        Parser::eat(parser, TokenType::CHAR_LITERAL);

        return new(&parser->arena, literal);
    }
}
