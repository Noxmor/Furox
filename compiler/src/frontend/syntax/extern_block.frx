export import "../ast/extern_block.frx";
import "func_decl.frx";
import "../core/parser.frx";
import "../../core/list.frx";

namespace AST::ExternBlock
{
    export ExternBlock* parse(Parser* parser)
    {
        frx::assert(parser != nullptr);

        ExternBlock* extern_block = new(&parser->arena);

        Parser::eat(parser, TokenType::KW_EXTERN);

        Parser::eat(parser, TokenType::LBRACE);

        while(Parser::loop_until(parser, TokenType::RBRACE))
        {
            list_push(&extern_block->func_decls, AST::FuncDecl::parse(parser));
        }

        Parser::eat(parser, TokenType::RBRACE);

        return extern_block;
    }
}
